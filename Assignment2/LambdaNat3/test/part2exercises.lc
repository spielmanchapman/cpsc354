// Uncomment any one of the following lines or paste one of the comments in the command line
// Do not run the interpreter without predicting a result first

//
// Parsing Tests (from PATH ../grammar)
//

//echo "(\ x . if x = 0 then 0 else S x) S 0" | ./TestLambdaNat


//
// Interpreter Tests
//

///// testing Conditional statement --> if else then
(\ x . if x = 0 then 0 else S x) 0
//(\ x . if x = 0 then 0 else S x) S 0

// (\x.x) a          //echo "(\x.x)a" | stack exec LambdaNat-exe


